@{
    if (ViewBag.Title == null)
    {
        ViewBag.Title = "Mon CV";
    }

    if (ViewBag.PageTitle == null)
    {
        ViewBag.PageTitle = "Bienvenue";
    }

    AspProject.Controllers.HomeController.myMenu.Add("Accueil","/home/index");
    AspProject.Controllers.HomeController.myMenu.Add("Utilisateur", "/home/users");
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>@ViewBag.Title - @ViewBag.PageTitle</title>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" />
    <link rel="stylesheet" href="~/Content/css/style.css" />

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    @MesHelpers.TopBar("Accueil",AspProject.Controllers.HomeController.myMenu)
    @*<header id="header">
        <nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="/home"><img id="logo" src="~/Content/img/crm.svg" /></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="/home/about"> <i class="fa fa-home"></i> Home <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="/user/index"> <i class="fa fa-users"></i> Users <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="/experience/index"> <i class="fa fa-book"></i> Experience <span class="sr-only">(current)</span></a>
                    </li>
                </ul>
                <ul class="navbar-nav mr-0">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="menuUser" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="fa fa-user"></i> Identification
                        </a>
                        <div class="dropdown-menu" aria-labelledby="menuUser">
                            <a class="dropdown-item" href="#">S'identifier</a>
                        </div>
                    </li>
                </ul>
            </div>
        </nav>
    </header>*@

    <main id="main" class="container">

        <h1>@ViewBag.Title</h1>

        @RenderBody()

    </main>

    <footer id="footer">
        <div class="row">
            <div class="col text-center">
                Copyright &copy; @DateTime.Now.Year.ToString()
            </div>
        </div>
    </footer>
</body>
</html>